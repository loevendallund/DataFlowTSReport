\begin{tabular}{l}
	\inference[$let$]
	{
		\inference[$Case$]{}
		{\Gamma;\Pi\vdash [caseEx]:T}
		\;\;
		\inference[$Ref\;read$]
		{
			\inference[$Var$]{}{\Gamma;\Pi\vdash x:(\{x\},\{\nu x\})}
		}
		{\Gamma,z:T;\Pi\vdash [!x]:T'}
	}
{\Gamma;\Pi\vdash [\mbox{let}\;z\;(caseEx)\;(!x)]:T'}\\
Where $T'=(\{x,\nu x\},\emptyset)$
\end{tabular}
